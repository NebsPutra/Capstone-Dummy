# Komunitas — Community Social Activity Platform

A mobile-first web app for discovering, creating, and joining local social
activities (running, reading, cycling, badminton, basketball, community
gatherings, and more). Built as an MVP prototype per the product spec.

## Stack

- **Next.js 16 (App Router) + React 19 + TypeScript**
- **Tailwind CSS** — warm Orange/Cream design system
- **Supabase** — Postgres, Auth, Row Level Security
- **Leaflet + OpenStreetMap** — maps, no API key required
- **Vercel** — deployment target

## 1. Set up Supabase

1. Create a project at [supabase.com](https://supabase.com).
2. In the SQL Editor, run these files **in order**:
   1. `supabase/schema.sql`: tables, enums, base RLS policies and helper functions.
   2. `supabase/migrations/002_auth_location_events.sql`: auth/onboarding,
      structured location, event status/capacity fixes, join/approval
      functions, nearby search and tighter RLS.
   For an **existing** project, run only step 2. It is idempotent and keeps
   existing data (hobbies, categories, events, participants, profiles).
3. In **Project Settings → API**, copy the Project URL and anon public key.

## 2. Configure Supabase Auth (email verification codes)

Sign in and sign up use 6-digit codes sent by email, so Auth needs:

- **Authentication → Providers → Email**: enable *Confirm email*, set the
  *Email OTP Expiration* to something short (e.g. `600` seconds) and the OTP
  length to 6.
- **Authentication → Email Templates**: the **Confirm signup**, **Magic Link**
  and **Reset Password** templates must include the code, e.g.
  `<p>Your Komunitas code: <strong>{{ .Token }}</strong></p>`.
  Sign-in codes use the Magic Link template.
- **Custom SMTP** (Project Settings → Authentication → SMTP): Supabase's
  built-in mailer only delivers to your own team's addresses and is heavily
  rate-limited. Set up a real SMTP provider before inviting users.

## 3. Configure environment variables

```bash
cp .env.local.example .env.local
```

Fill in:

```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

## 4. Install and run

```bash
npm install
npm run dev
```

Visit `http://localhost:3000`.

## 5. Deploy

Push to GitHub, then import the repo in Vercel. Add the same two
environment variables in the Vercel project settings. Vercel auto-detects
Next.js — no extra build config needed.

---

## How the spec maps to this codebase

| Spec section | Implementation |
|---|---|
| §2–3 Auth & Registration | `src/app/login` (email → password → emailed code), `src/app/register` (account → verify code → profile → hobbies), `src/app/forgot-password` |
| §4–5 Interests & Profile | `interests` / `user_interests` tables, `set_user_interests()` SQL function, `src/app/(app)/profile` |
| §6–8 Design system | `tailwind.config.ts`, `globals.css`: Orange/Cream tokens, Plus Jakarta Sans |
| §9 Layout / nav | `src/components/Sidebar.tsx` (collapsible), `MobileNav.tsx` (bottom nav on mobile) |
| §10 Landing page | `src/app/page.tsx` |
| §11 Dashboard | `src/app/(app)/dashboard` + `NearbyDashboard.tsx`: activities within 20 km via the `nearby_events()` SQL function |
| §12–13 GPS & privacy | `src/lib/location.ts`: live GPS or a manually chosen area (per device); precise GPS is never stored server-side |
| §14 Map | `MapPicker.tsx` (create/edit), `EventMap.tsx` (read-only, event details) |
| §15–17 Create / Edit Activity | `src/components/ActivityForm.tsx`, `src/app/(app)/create`, `src/app/(app)/activities/[id]/edit` |
| §18–19 Privacy / event codes | `event_code` and `share_token` generated by a DB trigger; `join_event()` enforces privacy, approval and capacity |
| §20 Sharing | `ShareBox.tsx`: invite link `/join/<token>`, WhatsApp, QR code |
| §22–23 Explore / search | `src/app/(app)/explore`: any distance; search by name, place or event code |
| §24 Event details | `src/app/(app)/activities/[id]`, also reachable via `/event/<id>`, `/event/code/<code>`, `/join/<token>` |

### Authentication flow

- **Sign in**: (1) email, where `auth_email_status()` says whether an account
  exists; (2) password, checked with Supabase Auth on a throwaway client so
  no session exists yet; (3) a 6-digit code is emailed (`signInWithOtp`), and
  verifying it creates the session.
- **Sign up**: the auth account is created and verified first, and a DB
  trigger creates the `profiles` row. Personal info and hobbies come after.
  The user only counts as registered once `set_user_interests()` sets
  `profiles.onboarding_completed_at`. Signed-in users with unfinished
  registration are sent back to the right step (`/register?resume=1`).
- Accounts from the old username flow used synthetic
  `@users.komunitas.app` emails, which can't receive codes. Those users need
  to register again with a real email.

### Location privacy

The browser's live GPS position is used at request time only and is never
saved to the database. If GPS is unavailable or denied, the user can pick
City → Kecamatan → Kelurahan instead, and its approximate centre (from
OpenStreetMap Nominatim) is used. The choice is remembered per device, so the
permission prompt isn't shown repeatedly. `profiles.area_lat/area_lng` only
store the registered kelurahan's centre, offered as a one-tap fallback.

### Localization

English and Indonesian strings live in `src/lib/i18n/translations.ts`; the
Indonesian dictionary is type-checked against the English keys. The choice is
stored in a `komunitas-lang` cookie so server-rendered pages use it too.
Database values (statuses, gender, categories, roles) stay as stable keys and
are translated only in the UI.

## Known gaps / next steps

- **Admin moderation**: admins can cancel events from the Admin Dashboard.
  User management, a `reports` table and category management aren't built.
- **Image uploads** (banner, avatar): no Supabase Storage bucket yet.
- **Strength of the second factor**: the app enforces password → email code,
  but Supabase's email-code endpoint can also be called directly with the
  public anon key. Someone with access to a user's inbox could sign in without
  the password. Closing that needs a server-side login endpoint or Supabase MFA.
- **Email enumeration**: sign-in step 1 says whether an account exists (as the
  spec requires). Supabase's auth rate limits still apply to later steps.
- **PIC WhatsApp privacy**: the UI only shows it when public or to approved
  participants, but the column is still readable through the API for public
  events. Hiding it fully needs column-level grants or a view.
- **Reminders**: there are no scheduled event reminders or push notifications.

## Project structure

```
src/
  app/
    page.tsx                  Landing page
    login/, register/         Auth
    (app)/                    Authenticated shell (sidebar + header + mobile nav)
      dashboard/
      explore/
      create/
      activities/[id]/        Event details (+ /edit)
      join/, event/           Invite link / event code resolvers
      profile/
      my-activities/
      community/
      notifications/
      settings/
    admin/                    Admin dashboard
  components/                 Shared UI (cards, map, nav, join/share widgets)
  lib/supabase/               Browser + server Supabase clients
  lib/utils.ts                Distance, date, currency formatting
  types/                      Shared TypeScript types
supabase/schema.sql           Base DB schema + RLS policies
supabase/migrations/          Patches to run after schema.sql, in order
```
